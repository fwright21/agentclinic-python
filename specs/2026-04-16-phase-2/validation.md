# Phase 2 — Validation

## Checks

- [ ] `GET /` returns 200 and renders HTML (not raw JSON)
- [ ] Page has correct `<title>` and links to `style.css`
- [ ] Header visible: AgentClinic name left, nav links right
- [ ] Nav contains: Agents, Ailments, Appointments, Dashboard (all href="#")
- [ ] Footer visible: app name + version
- [ ] CSS custom properties defined for all colour tokens (base, status, severity)
- [ ] Inter font loading (check in browser)
- [ ] Static files served: `GET /static/css/style.css` returns 200
- [ ] Page background is `#F8FAFC`, text is `#0F172A`

## Done when
All boxes checked and layout renders correctly in browser at `http://localhost:8000/`.
